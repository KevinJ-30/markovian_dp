{
  "dataset": "ppi",
  "method": "dpar",
  "seed": 0,
  "device": "cpu",
  "split_strategy": "native",
  "split_root": "data/inductive_splits/ppi_native",
  "multilabel": true,
  "parameters": {
    "epochs": 2,
    "batch_size": 32,
    "hidden_size": 32,
    "layers": 2,
    "dropout": 0.0,
    "learning_rate": 0.01,
    "weight_decay": 0.0,
    "sampled_train_nodes": 150,
    "topk": 8
  }
}
