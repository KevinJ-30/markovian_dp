{
  "dataset": "cora-ml",
  "method": "mlp",
  "seed": 0,
  "device": "auto",
  "split_root": "data/inductive_splits",
  "parameters": {
    "epochs": 100,
    "hidden_size": 64,
    "layers": 2,
    "dropout": 0.5,
    "learning_rate": 0.01,
    "weight_decay": 0.0005
  }
}
