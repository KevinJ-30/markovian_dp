{
  "dataset": "ogbn-products",
  "method": "mlp",
  "seed": 0,
  "device": "cuda:5",
  "split_strategy": "native",
  "split_root": "data/inductive_splits/ogbn_products_native",
  "parameters": {
    "epochs": 1,
    "hidden_size": 32,
    "layers": 2,
    "dropout": 0.0,
    "learning_rate": 0.01,
    "weight_decay": 0.0005
  }
}
